{
  "name": "@hooklusion/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "hooklusion": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
